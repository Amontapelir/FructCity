## Что меняем и почему

<!-- Одним абзацем: что было не так или чего не хватало. «Почему» важнее
     «что» — «что» видно из диффа. -->

Закрывает #

## Проверка

- [ ] `python -m unittest discover -s backend/tests -t .` — зелёный
- [ ] Новое поведение закрыто тестом
- [ ] Тест проверен подменой: **без изменения он краснеет**
- [ ] Схема базы не менялась ИЛИ есть ревизия alembic и `test_migrations.py` зелёный
- [ ] Контракт API не изменился ИЛИ витрина (`public/app.js`) поправлена в этом же PR
- [ ] Комментарии и docstring обновлены, если менялись причины решений

## Что могло сломаться

<!-- Чаще всего задевается одно из трёх:
     - деньги считаются одним кодом на сервере (domain/calc.py) и в
       браузере (lib/calc.js) — правишь формулу, правь оба;
     - изменение состояния идёт только внутри транзакции;
     - контракт API, включая формат чисел: 99, а не 99.0.
     Если задето — назвать и объяснить, почему это безопасно. -->

Ничего из перечисленного.
